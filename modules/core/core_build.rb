# core_build.rb
# Builds the core assemblies for Apollo

require 'rake/clean'

# Define which files to clean
CLEAN = FileList[]

# Define which files to delete in the clobber execution
CLOBBER = FileList[]

# Define the solution files
SRC = FileList[]

# Define the final BIN directory


# Define the dependencies